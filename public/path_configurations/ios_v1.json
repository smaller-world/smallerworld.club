{
  "settings": {},
  "rules": [
    {
      "patterns": ["^/spaces$", "^/login"],
      "properties": {
        "presentation": "replace_root"
      }
    },
    {
      "patterns": ["^/settings$", "/new$", "/edit$"],
      "properties": {
        "context": "modal",
        "pull_to_refresh_enabled": false
      }
    }
  ]
}
