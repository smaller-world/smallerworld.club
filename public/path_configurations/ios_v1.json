{
  "settings": {},
  "rules": [
    {
      "patterns": ["^/spaces$", "^/world$", "^/universe$"],
      "properties": {
        "presentation": "replace_root"
      }
    },
    {
      "patterns": ["^/login"],
      "properties": {
        "context": "modal",
        "presentation": "replace",
        "modal_dismiss_gesture_enabled": false
      }
    },
    {
      "patterns": ["^/settings$", "/new$", "/edit$"],
      "properties": {
        "context": "modal",
        "pull_to_refresh_enabled": false
      }
    }
  ]
}
