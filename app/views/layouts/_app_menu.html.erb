<div data-controller="dropdown" class="relative">
  <button
    type="button"
    class="btn gap-1 py-1 pr-3 pl-2"
    data-dropdown-target="trigger"
    data-action="dropdown#toggle"
    aria-haspopup="menu"
  >
    <%= vite_image_tag "assets/images/logo.png", class: "size-6" %>
    <span class="text-lg font-bold">smaller world</span>
    <%= icon "chevron-down", variant: :micro, class: "size-4 ml-0.5" %>
  </button>

  <div
    class="dropdown_menu dropdown_menu-top card menu_card hidden"
    data-dropdown-target="menu"
  >
    <% if (user = current_user) %>
      <div
        class="
          font-headings text-shade-dimmed px-2 py-1 text-center text-xs
          leading-none font-light
        "
      >
        signed in as
        <span class="text-shade-500 dark:text-shade-400 font-medium"><%= user.name %></span>
      </div>
    <% end %>

    <ul role="menu" class="space-y-0.5">
      <% if signed_in? %>
        <%= render "layouts/app_menu/contact_us_item" %>
        <li role="presentation" class="py-1">
          <div role="separator"></div>
        </li>
        <li role="presentation">
          <%= button_to(
            destroy_session_path,
            role: "menuitem",
            form_class: "flex flex-col",
            class: "text-destructive-600 dark:text-destructive-400",
            data: {
              action: "dropdown#toggle",
            },
          ) do %>
            <%= icon "arrow-left-start-on-rectangle", variant: :micro, class: "size-4" %>
            <span>sign out</span>
          <% end %>
        </li>
      <% else %>
        <li role="presentation">
          <%= link_to(
            new_session_path,
            role: "menuitem",
            data: {
              action: "dropdown#toggle",
            },
          ) do %>
            <%= icon "arrow-right-end-on-rectangle", variant: :micro, class: "size-4" %>
            <span>sign in</span>
          <% end %>
        </li>
        <li role="separator" class="py-1">
          <div role="separator"></div>
        </li>
        <%= render "layouts/app_menu/contact_us_item" %>
      <% end %>
    </ul>
  </div>
</div>
