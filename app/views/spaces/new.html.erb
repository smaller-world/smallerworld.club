<div class="page-container flex flex-col gap-6">
  <%= back_link_to "your spaces", spaces_path %>

  <%= turbo_frame_tag dom_id(@space, :new), target: "_top" do %>
    <%= render "spaces/form", space: @space %>
  <% end %>
</div>
