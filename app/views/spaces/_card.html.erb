<div id="<%= dom_id(space, :card) %>" class="card flex gap-4">
  <% if space.icon.attached? %>
    <%= image_tag space.icon, class: "rounded-app-icon size-20" %>
  <% end %>
  <div class="flex-1 flex flex-col gap-y-0.5">
    <p class="font-headings font-semibold text-lg leading-tight">
      <%= space.name %>
    </p>
    <p class="text-shade-500 leading-tight">
      <%= space.description %>
    </p>
  </div>
</div>
