postgres: bin/postgres
mailcatcher: bin/mailcatcher
vite: bin/vite dev
build_ssr: bin/vite build --ssr -m dev --watch --force
ssr: sleep 4 && bundle exec rerun -p 'dist/ssr/**/*.js' -q -- bin/vite ssr
rails: bin/wait_for_postgres_ready && bin/rails server -b 0.0.0.0
